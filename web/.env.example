# Spring Boot user_service (default port 8081)
VITE_USER_SERVICE_URL=http://localhost:8081

# Spring Boot integration_service (default port 8082)
VITE_INTEGRATION_SERVICE_URL=http://localhost:8082

# Set to true to use the in-memory mock instead of the API (offline dev)
# VITE_USE_MOCK_AUTH=false
